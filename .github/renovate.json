{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "labels": ["dependencies"],
  "packageRules": [
    {
      "description": "Group all non-major Maven updates into a single PR weekly",
      "matchDatasources": ["maven"],
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "Maven dependencies (non-major)",
      "schedule": ["before 8am on Monday"]
    },
    {
      "description": "Major updates as individual PRs for review",
      "matchDatasources": ["maven"],
      "matchUpdateTypes": ["major"],
      "groupName": null
    },
    {
      "description": "Auto-merge patch updates if CI passes",
      "matchUpdateTypes": ["patch"],
      "automerge": true,
      "automergeType": "pr",
      "platformAutomerge": true
    },
    {
      "description": "Java 22 - reject updates that change JDK version",
      "matchPackageNames": ["java", "jdk"],
      "enabled": false
    }
  ],
  "vulnerabilityAlerts": {
    "enabled": true,
    "labels": ["security"]
  }
}
